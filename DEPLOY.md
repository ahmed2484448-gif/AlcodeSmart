# الكود الوسيط — نسخة الويب (تعمل على الآيفون)

هذه هي ملفات التطبيق كموقع ويب ثابت (Static). تعمل كما هي بدون خادم خلفي وبدون قاعدة بيانات —
كل البيانات تُحفظ في متصفح الجهاز (localStorage + IndexedDB).

## المحتوى

```
index.html          الصفحة الوحيدة
style.css            التصميم
manifest.json        إعدادات التثبيت (PWA)
service-worker.js    التشغيل بدون إنترنت
icon.png             الأيقونة (512×512)
images/1.png 2.png 3.png   شعارات الخدمات
idb.js data.js hisn.js lock.js script.js
contacts.js history.js stats.js athkar.js fx.js native.js
```

`native.js` يستدعي واجهات أندرويد (بصمة/اختصارات) — على الويب تُتجاهَل تلقائيًا بدون أخطاء.

## النشر (أي خيار يكفي)

1. **Netlify Drop** — اذهب إلى https://app.netlify.com/drop واسحب مجلد `www` كاملًا.
   تحصل على رابط `https://اسم-عشوائي.netlify.app` خلال ثوانٍ.
2. **GitHub Pages** — ارفع محتوى `www` إلى مستودع، ثم Settings → Pages → Deploy from branch.
3. **Cloudflare Pages / Vercel** — نفس الفكرة، اسحب المجلد أو اربط المستودع.
4. **أي استضافة عادية** — ارفع الملفات إلى `public_html`.

> مهم: يجب أن يكون الموقع على **HTTPS** حتى يعمل التثبيت وبصمة الويب والحفظ المحلي.
> (كل الخيارات أعلاه تعطي HTTPS تلقائيًا.)

## على الآيفون

1. افتح الرابط في **Safari** (وليس Chrome).
2. اضغط زر المشاركة (المربع مع السهم لأعلى).
3. اختر **«إضافة إلى الشاشة الرئيسية»**.
4. يظهر التطبيق بأيقونته ويعمل بملء الشاشة وبدون إنترنت.

على أندرويد: افتح الرابط في Chrome → يظهر زر «تثبيت التطبيق» تلقائيًا.

## نسخة iOS أصلية (App Store)

تحتاج جهاز Mac + Xcode. المشروع أصلًا مغلّف بـ Capacitor:

```bash
npm install
npx cap add ios          # يُنشئ مجلد ios/
npx cap sync ios
npx cap open ios         # يفتح Xcode للبناء والرفع
```

(مجلد `ios/` جاهز في مشروع Capacitor الرئيسي مع صلاحيات ووصف الاستخدام.)

## ملاحظات

- لتغيير الأيقونة أو الألوان: عدّل `icon.png` و `style.css` ثم أعد النشر.
- البيانات لا تُرفع لأي خادم — نسخة الويب ونسخة أندرويد لا تتشاركان البيانات.
- ميزة «تصدير/استعادة نسخة احتياطية» داخل التطبيق تنقل البيانات بين الأجهزة عبر ملف.
